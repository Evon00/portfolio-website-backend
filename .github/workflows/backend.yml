name: Deploy Backend

on:
  push:
    branches: ["dev","main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Create application.yml from Secret
        run: |
          mkdir -p ./src/main/resources
          echo "${{ secrets.APPLICATION_YML }}" > ./src/main/resources/application.yml

      - name: Log in to DockerHub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Set up Docker Buildx
        run: docker buildx create --use

      - name: Build & Push Multi-Arch Backend Docker Image
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/backend:latest \
            --push .

      - name: Deploy Backend to EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ${{ secrets.EC2_APP_PATH }}
            cd ${{ secrets.EC2_APP_PATH }}
            
            echo "Stopping backend service..."
            docker-compose stop backend
            
            echo "Removing old backend container and image..."
            docker-compose rm -f backend
            docker rmi ${{ secrets.DOCKERHUB_USERNAME }}/backend:latest || true
            
            echo "Pulling fresh backend image..."
            docker-compose pull backend
            
            echo "Starting new backend..."
            docker-compose up -d backend
            
            echo "Cleaning up..."
            docker image prune -f
            
            echo "Backend deployment completed successfully"
